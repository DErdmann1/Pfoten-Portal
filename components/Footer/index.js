import styled from "styled-components";

const StyledFooter = styled.footer`
  text-align: center;
  border-top: 2px solid black;
`;

export default function Footer() {
  return <StyledFooter>©PfotenPortal</StyledFooter>;
}
